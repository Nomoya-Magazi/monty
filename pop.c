#include "monty.h"
/**
 * f_pop - the function that removes the top element of the stack
 * @head: the stack head
 * @counter:line_number
 * Return: no return
 */
void f_pop(stack_t **head, unsigned int counter)
{

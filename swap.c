#include "monty.h"
/**
 * f_swap -opcode that swaps the top two elements of the stack
 * @head: the top of the stack
 * @counter; line_number
 * Return: no return
 */
void f_swap(stack_t **head, unsigned int counter)
{

#include "monty.h"
/**
 * f_nop - The opcode that doesn’t do anything.
 * @head: top of stack
 * @counter: no counter
 * Return: no return
 */
void f_nop(stack_t **head, unsigned int counter)
{
(void) counter;
	(void) head;
}
